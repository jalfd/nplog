@PACKAGE_INIT@

find_package(Threads REQUIRED)

if(NOT TARGET np::log)
    list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}")
    include("${CMAKE_CURRENT_LIST_DIR}/nplog-targets.cmake")
endif()